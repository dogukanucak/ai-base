# Embedding
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2
LOCAL_MODEL_PATH=/Users/dogukanucak/Projects/AI-Projects/ai-base/models/all-MiniLM-L6-v2

# OpenAI
USE_OPENAI=false
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.7

# University Search
UNIVERSITY_SEARCH_URL=https://www.ieu.edu.tr/anasayfa/ajax/search.php

# Chunking
CHUNKING_TYPE=markdown
CHUNK_SIZE=300
CHUNK_OVERLAP=50
CHUNK_SEPARATORS=["###", "##", "#"]
TOKEN_ENCODING=cl100k_base
TOKEN_BUDGET=2000

# Retrieval
RETRIEVAL_TYPE=similarity
FETCH_K=3
SCORE_THRESHOLD=0.5

# Vector Store
COLLECTION_NAME=documents
CHROMA_URL=http://localhost:8000

# Document Loader
DOCUMENTS_PATH=../../docs
LOAD_DOCUMENTS=true

# Console
CONSOLE_MAX_RESPONSE_LENGTH=10000
SHOW_DEBUG_INFO=true
TRUNCATE_DOCUMENTS=false
DOCUMENT_PREVIEW_LENGTH=10000

# Vector Store Configuration
SIMILARITY_THRESHOLD=0.5
VECTOR_STORE_PATH=data/vectors.json 
